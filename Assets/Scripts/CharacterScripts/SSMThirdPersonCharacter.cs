using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SSMThirdPersonCharacter : MonoBehaviour {

    [SerializeField]
    private float turnSpeed, movingTurnSpeed, JumpForce, runCycleOffset, movementMultiplier, animatorSpeedMultiplier, groundDist;
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
